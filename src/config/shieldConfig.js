export const ShieldModelConfig = Object.freeze({
  maxHealth: 25,
  maxStrength: 30,
  rechargeRatePerSecond: 2,
  energyPerStrength: 1,
  heatPerAbsorbedDamage: 2
});

export const ShieldViewConfig = Object.freeze({
  localX: 0,
  localY: 0,
  radiusOffset: 10
});
